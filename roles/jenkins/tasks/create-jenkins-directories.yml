---
- name: Create data directory
  file:
    state: directory
    path: /data
    owner: root
    group: root

- name: Create jenkins data directory
  file:
    state: directory
    path: /data/jenkins
    owner: 1000
    group: 1000